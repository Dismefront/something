import { ReactElement } from "react";
import '../styles.scss';

const Home = (): ReactElement => {
    return (
        <div className="fgee2">
            And this is my title
        </div>
    );
};

export default Home;